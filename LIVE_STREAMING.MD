Before writing any code, explore the full codebase thoroughly. Find the existing Cloudflare Stream integration already in place for video uploads, including the API credentials, the server side API routes, the environment variables CLOUDFLARE_STREAM_API_TOKEN and CLOUDFLARE_ACCOUNT_ID, and the existing webhook setup. Find the existing push notification infrastructure and which service is used. Find the existing premium and unlimited tier gating logic. Find the existing follower count logic in the database. Find the existing profile and community screens on both the React Native app and the Next.js web app. Do not duplicate anything already in place. Match all new code to existing patterns and conventions.

You are implementing a Live Streaming feature for SoundBridge using Cloudflare Stream Live. Here are the full requirements:

ACCESS CONTROL: Live streaming is not available to all creators at this stage. It is available only to creators who meet one of the following conditions. They have 50 or more followers on SoundBridge. Or they are a designated partner, specifically users whose accounts are tagged with a partner institution such as logic_church, loud_urban_choir, or any other partner identifier already stored in the database. Check how partner identifiers are stored and used for existing institutional partners and use that same pattern to check eligibility. If a creator does not meet either condition, show them a message explaining that live streaming is available to creators with 50 or more followers or to designated SoundBridge partners, and encourage them to grow their following.

INFRASTRUCTURE: Use Cloudflare Stream Live for all live streaming. When an eligible creator starts a live stream, generate a Cloudflare Stream Live input via the server side API route using the existing Cloudflare credentials. Return the RTMP URL and stream key to the creator for broadcast. Deliver the stream to viewers using the Cloudflare Stream HLS player already used for video playback. Do not introduce any new streaming service or library. Do not expose Cloudflare API tokens client side under any circumstances.

STREAM LIMITS: Maximum stream duration is 3 hours. At 2 hours and 45 minutes send the creator an in-app warning that their stream will end in 15 minutes. At 3 hours automatically end the stream via the Cloudflare Stream API. Store stream start time in Supabase and use a server side scheduled check or webhook to enforce the time limit. Do not rely on client side timers for enforcement.

DATABASE: Create a live_streams table in Supabase if it does not already exist with these fields. id uuid primary key default uuid_generate_v4(). user_id uuid references profiles(id) on delete cascade. cloudflare_stream_id text not null. status text with check constraint allowing only the values active, ended and failed. started_at timestamptz default now(). ended_at timestamptz nullable. title text nullable. viewer_count integer default 0. created_at timestamptz default now(). Apply Row Level Security. Creators can only manage their own streams. Authenticated users can read active streams. Check the existing schema before creating this table.

GOING LIVE FLOW: The creator taps a Go Live button on their profile or a dedicated Live tab. They enter an optional stream title. The app calls a server side API route which checks eligibility, creates a Cloudflare Stream Live input, saves a record to the live_streams table with status active, and returns the RTMP URL and stream key. Display the RTMP URL and stream key to the creator clearly so they can use it with their broadcasting software or the in-app broadcast option if one exists. Show a live indicator on the creator's profile visible to all users while the stream is active.

VIEWER EXPERIENCE: When a viewer opens a creator's profile while they are live, show a prominent live banner and open the Cloudflare Stream HLS player automatically. Use the existing Cloudflare Stream player component already implemented for video uploads. Show a live viewer count that updates in real time using Supabase Realtime. Allow viewers to send tips during the live stream using the existing tip infrastructure already in place. Tips sent during a live stream should be processed identically to existing tips with the same platform fee and creator payout split.

PUSH NOTIFICATIONS: When an eligible creator goes live, send a push notification to all their followers notifying them. Use the existing push notification infrastructure already in the codebase. Check which service and pattern is already used and follow it exactly. Do not introduce any new notification library.

STREAM ENDING: When a stream ends whether by the creator manually ending it, the 3 hour limit being reached, or a Cloudflare webhook indicating the stream has disconnected, update the live_streams record status to ended and set ended_at. Remove the live indicator from the creator's profile. Send the creator a summary notification showing how long they streamed and the total tips received during the session.

ERROR HANDLING: If stream creation fails, show the creator a friendly message saying the stream could not be started and to try again. Never show raw errors, JSON or technical output. If the stream disconnects unexpectedly, attempt to detect this via Cloudflare webhooks and notify the creator. If any backend error occurs show this exact message to the creator: We are currently running some improvements to our live feature. Please bear with us and try again shortly.

MOBILE AND WEB PARITY: Every part of this feature must work on both the React Native Expo mobile app and the Next.js web app. Going live, viewing a live stream, tipping during a live stream, push notifications and the live indicator on profiles must all function correctly on both platforms.