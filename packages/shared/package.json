{
  "name": "@soundbridge/shared",
  "version": "1.0.0",
  "description": "Shared business logic and utilities for SoundBridge platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@soundbridge/types": "file:../types",
    "@soundbridge/supabase": "file:../supabase"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ]
}
